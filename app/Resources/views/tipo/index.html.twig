{% extends 'final.html.twig' %}

{% block main %}
    <div class="container">
        {% for flash_message in app.session.flashBag.get('success') %}
            <div class="alert alert-success" role="alert">
                {{ flash_message }}
            </div>
        {% endfor %}
        <div class="row">
            <div class="col-md-12">
                <div class="row">
                    <div class="col-md-8">
                        {% if is_granted('ROLE_ADMIN') %}
                            <h2>Administracion de tipo</h2>
                        {% else %}
                            <h2>Ver tipos</h2>
                        {% endif %}
                    </div>
                    <div class="col-md-4">
                        {% if is_granted('ROLE_ADMIN') %}
                            <!-- Boton nuevo tipo -->
                            <div class="panel panel-default">
                                <div class="panel-body">
                                    <a href="{{ path('tipo_create') }}">
                                        <div class="btn btn-primary btn-lg btn-block">Nuevo tipo
                                        </div>
                                    </a>
                                </div>
                            </div>
                        {% endif %}
                    </div>
                </div>

                <!-- Tabla de tipos -->
                <div class="table-responsive">
                    <table class="table table-bordered table-hover table-striped">
                        <thead>
                        <th>Seleccionar</th>
                        <th>Nombre</th>
                        </thead>
                        {% for tipo in tipos %}
                            <tr>
                                <td class="text-center"><a href="{{ path('tipo_show', { id: tipo.id }) }}"><span
                                                class="glyphicon glyphicon-edit"
                                                aria-hidden="true"></span></a></td>
                                <td>{{ tipo.nombre }}</td>
                            </tr>
                        {% endfor %}
                    </table>
                </div>


            </div>
        </div>
    </div>
{% endblock %}